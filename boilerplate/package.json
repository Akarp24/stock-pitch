{
  "name": "stock-research-portal",
  "version": "1.0.0",
  "description": "Institutional equity research portal — Cloudflare Worker scaffold",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
